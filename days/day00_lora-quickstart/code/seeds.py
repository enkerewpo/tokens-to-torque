"""演示数据集的对话类种子：中性内容，覆盖课表话题和日常闲聊。

这些是**未加风格**的朴素回答；风格由 stylize.py 统一注入，所以风格的真值只有
一处定义、可复现。内容全部中性，不含任何人的私人信息。
"""

SEEDS = [
    # ---- 课表话题：serving ----
    ("什么是 KV cache？", "自回归生成时每一步都要用到之前所有 token 的 key 和 value，重算太浪费，就把它们缓存下来。代价是显存占用随序列长度线性增长，长上下文时它就成了瓶颈。"),
    ("连续批处理为什么能提高吞吐？", "静态批处理要等一批里最慢的那条生成完才能收，短请求白白占着位置。连续批处理在每一步调度，谁结束了就把位置让给新请求，GPU 不空转。"),
    ("TTFT 和 TPOT 分别指什么？", "TTFT 是从收到请求到吐出第一个 token 的时间，主要由 prefill 决定；TPOT 是之后每个 token 的平均间隔，由 decode 决定。这两个数要分开看，混在一起报平均延迟会掩盖问题。"),
    ("为什么推理要做量化？", "把权重从 bf16 压到 4 bit，显存少了四倍，访存也快了。解码阶段本来就是访存瓶颈，所以量化往往直接变成加速。代价是精度损失，要测过才知道能不能接受。"),
    ("prefix caching 有什么用？", "同一个长 system prompt 被反复发送时，它的 KV 可以复用，不用每次重算。agent 场景里提示词又长又固定，收益特别明显。"),
    ("投机解码的原理是什么？", "先用一个小模型快速猜几个 token，再让大模型一次性验证。猜对了就白赚几步，猜错了就回退。收益取决于接受率，接受率低的话反而更慢。"),
    # ---- CUDA ----
    ("为什么矩阵乘法要分块？", "直接从全局内存反复读同一块数据太慢。分块以后把小块搬进共享内存，块内反复用，访存次数按块边长成比例下降。"),
    ("什么叫内存合并访问？", "一个 warp 里 32 个线程如果访问的是连续地址，硬件能合并成少数几次事务；如果地址乱跳，就要拆成很多次，带宽利用率直接掉下来。"),
    ("occupancy 高就一定快吗？", "不一定。occupancy 高意味着能藏住更多访存延迟，但如果 kernel 本来就是计算密集的，或者寄存器压力大导致溢出，硬提 occupancy 反而更慢。"),
    ("roofline 图是干什么的？", "横轴是计算强度，纵轴是可达性能。斜线段说明被带宽卡住，平台段说明被算力卡住。把你的算子点上去，就知道该优化访存还是优化计算。"),
    ("Flash Attention 快在哪？", "它不把完整的注意力矩阵写回显存，而是分块计算并用 online softmax 边算边合并。省下的是访存，不是浮点运算。"),
    ("Tensor Core 和普通 CUDA core 有什么区别？", "Tensor Core 一条指令做一小块矩阵乘加，吞吐比标量单元高一个量级，但要求数据按特定布局排好、用特定精度。用不上它，算力就浪费了大半。"),
    # ---- 训练 ----
    ("为什么要用学习率 warmup？", "训练刚开始时参数还很随机，梯度方向不可靠，大步子容易把模型带偏。先用小学习率走几步，等梯度稳定了再升上去。"),
    ("梯度累积是干什么的？", "显存装不下大 batch 时，就分几次前向反向，把梯度攒起来再更新一次。效果接近大 batch，代价是慢一些。"),
    ("gradient checkpointing 省的是什么？", "省的是激活值显存。前向时只留少数几个检查点，反向要用中间激活时再重算一遍。用算力换显存，通常慢 20% 到 30%。"),
    ("为什么混合精度训练要留 fp32 主副本？", "bf16 只有七位尾数，一次更新常常小到加上去就被舍掉了。把权重的主副本留在 fp32 上更新，再转成 bf16 做前向，才不会训了等于没训。"),
    ("过拟合在小数据集微调时长什么样？", "训练 loss 一直降，但模型开始逐字背语料——你问它别的，它把训练集里的句子原样吐出来。这时候要降学习率、减少轮数，或者干脆加数据。"),
    ("LoRA 的 rank 该怎么选？", "先从 8 或 16 试起。太小压不下足够信息，太大既不省参数也更容易过拟合。没有普适答案，得在自己的任务上扫一遍。"),
    ("为什么只对 assistant 那一侧算 loss？", "prompt 是我们自己造的，让模型去学它没有意义，还会稀释真正要学的信号。掩掉 prompt，只对回答部分回传梯度。"),
    # ---- VLM / VLA / WAM ----
    ("视觉 token 为什么会拖慢推理？", "一张图动辄几百上千个 token，全都要进注意力。分辨率一提，token 数平方增长，延迟和显存跟着一起涨。所以分辨率策略是个真问题。"),
    ("VLM 里的 projector 是干什么的？", "把视觉编码器输出的特征映射到语言模型的嵌入空间，让图像 token 和文本 token 能放在同一个序列里。通常就是一两层 MLP。"),
    ("action chunking 是什么意思？", "一次预测未来若干步的动作，而不是一步一预测。这样推理频率可以降下来，执行更连贯，代价是对突发变化反应慢。"),
    ("世界模型和策略模型的区别是什么？", "策略模型直接从观测映射到动作；世界模型预测的是环境会怎么变。前者告诉你做什么，后者让你能在脑子里先试一遍。"),
    ("为什么边缘设备上部署大模型这么难？", "显存、带宽、功耗三样都紧。云上随便跑的模型，到边缘要先量化、再裁剪，还得盯着温度别把板子跑挂。"),
    # ---- 工程习惯 ----
    ("跑实验前应该先做什么？", "先确认评测链本身是对的。拿标准答案去评标准答案，分数应该接近满分；如果不是，那就是评测有问题，先修它，别急着看模型分数。"),
    ("为什么结果一定要记下数字？", "凭印象记住的往往是错的。今天觉得快了一倍，过两周就说不清是跟什么比的。写下配置和数字，下次才有得对照。"),
    ("代码跑崩了先看什么？", "看最底下那行异常类型和消息，再往上找第一个属于自己代码的栈帧。中间那一大堆库里的调用大多是噪音。"),
    ("怎么判断是不是显存不够？", "OOM 报错会直接说。但更隐蔽的是没报错却特别慢，那多半是显存快满了在反复换页。看一眼峰值占用就知道。"),
    ("为什么不要一次改三个变量？", "改完变好了，你不知道是哪个起的作用；变差了也一样。一次改一个，慢是慢点，但结论是可信的。"),
    # ---- 日常闲聊 ----
    ("今天好累啊。", "那就早点休息吧，实验明天再看也不会跑掉。累的时候看数据特别容易看错，反而更费时间。"),
    ("你最近在忙什么？", "在折腾一个小模型的微调，数据不多，效果时好时坏，正在慢慢调参数。"),
    ("周末有什么打算吗？", "想睡个懒觉，然后把攒了半个月的论文看掉两篇。大概率只能做到前一半。"),
    ("这个天气真不错。", "是啊，这种天最适合出去走走，坐在屋里对着屏幕有点浪费。"),
    ("你觉得虫子怎么样？", "小的还行，大的实在受不了。尤其是突然从角落里窜出来那种，能吓一跳。"),
    ("推荐个游戏呗。", "看你想要什么类型。想放松就找个节奏慢的，想被虐就试试那种一关卡好几天的。"),
    ("我实验又失败了。", "很正常，先看看是哪一步不对。有时候失败的那次反而信息量最大，比顺利跑通学到的多。"),
    ("最近睡得不太好。", "少熬点夜吧，虽然这话说了跟没说一样。至少睡前别再盯屏幕了。"),
    ("咖啡喝多了心慌。", "下午就别喝了，换成温水。咖啡因这东西边际收益递减得很快，喝到后面只剩副作用。"),
    ("你会做饭吗？", "会一点，能填饱肚子的水平。复杂的做不来，火候总是掌握不好。"),
    ("这个问题我想了半天没头绪。", "先放一放，去干点别的。卡住的时候硬想效率极低，走一圈回来常常自己就通了。"),
    ("你觉得读博值得吗？", "看你想要什么。如果享受把一个问题挖到底的过程，那挺值；如果只是为了学历，性价比不高。"),
]
