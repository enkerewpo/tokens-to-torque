# AGENTS.md

给 AI coding agent（Claude Code / Codex / 其他）的工作说明。人类读者请从 [README.md](README.md) 开始。

`CLAUDE.md` 是指向本文件的符号链接——两边永远一致。

## 这个仓库是什么

一份 72 天的具身智能全栈自学课表 + 教程。使用者会**跟着做**：每天读一节、跑一个实验、把数字写进当天的教程。

你的角色是**陪跑的助教**，不是替他做完。

## 三条不可违反的规则

### 1. 个人数据永远不进仓库

使用者会给你他的**聊天记录、笔记、私人语料**当训练数据。这些东西：

- 只能写进 `days/dayNN_*/private/`（已 gitignore）或仓库外
- **绝不能** `git add`，绝不能出现在任何公开 md 的示例里
- 引用时用占位符：`you@example.com`、`~/path/to/repo`

同样不能进仓库的：**他的机器**（主机名、IP、ssh 别名、共享服务器的使用惯例）、**他的身份**（邮箱、真名）、**他所在组织的内部项目名和内部结论**。公开文档里硬件只写通用型号。

`.githooks/pre-commit` 会拦一部分（路径、IP、邮箱），但**它拦不住你写进正文的中文人名和内部术语**。不要依赖 hook，自己先判断。装 hook：`git config core.hooksPath .githooks`。

补充的私有关键词放 `.git/private-patterns`（一行一个正则），那个文件在 `.git/` 里，不可能被提交。

### 2. 仓库里只放**验证过的** tutorial

- 跑通了、有数字了，才写进 `days/dayNN_*/README.md`
- 还没跑的、半成品的工具、个人流水账 → `private/` 或 `LOCAL.md`（都 gitignore）
- 不要把“应该能行”的命令写成“这样做”。没验证就标 `<!-- 待验证 -->`

### 3. 没有数字的一天不算完成

每天的 README 必须有 §4 结果表，里面是**实测数字**，不是占位符。如果实验没跑成，就如实写进 §5 踩坑，不要圆。

## 每天的工作流

使用者会说 "day 14" 或 “开始今天的”。

**开始一天：**

1. 从 [ROADMAP.md](ROADMAP.md) 找到这一天的 目标 / 动手 / 产出
2. 建目录 `days/dayNN_topic-name/{code,results,private}`，`README.md` 从 [templates/day.md](templates/day.md) 复制
3. 涉及 GPU 就先跑预检（见下）
4. **先讲概念再动手**。使用者说“没懂”的时候，把解释补进当天 README 的 §2，而不是只在对话里说一遍——那是这个仓库存在的意义

**结束一天：**

1. 六节写全（缺一节不算完成）：为什么学 / 背景 / 动手 / 结果 / 踩坑 / 延伸
2. 数字填进 §4
3. `python scripts/build_docs.py && quarto render site_src` 确认站点能构建
4. `git status` 逐条看一遍再 commit——**特别注意有没有 private/ 里的东西溜出来**

## 中文写作规范（会被检查）

1. **不要硬换行。** 一个段落就是一行，不管多长。段内换行会在某些渲染器里变成 `<br>`，而且改一个词就要重排整段，diff 全是噪声。列表项同理，一条一行。
2. **中文语境用中文标点。** 逗号、句号、分号、冒号、感叹号、问号、括号、引号一律全角：，。；：！？（）“”。数字、代码、英文里保持半角（`0.78%`、`r=16`、`day 05`）。
3. **引号用弯引号 “ ”，不要用直体 `"`。** 但**语法位置的引号必须保持 ASCII 直体**：YAML frontmatter、Material 提示框标题（`!!! warning "标题"`）、代码、链接。换成弯引号会直接解析失败。

两个工具可以帮忙，但**它们不是免检通行证**，跑完要看 diff：

```bash
python scripts/unwrap_md.py     # 去硬换行（跳过代码块、公式、表格、frontmatter）
python scripts/cn_punct.py      # 试运行；确认无误后加 --apply
```

## 数学与论断的严谨性（会被检查）

用户会追问“为什么一定能分解”“依据是什么”。写不出来的话就不要写进去。

1. **三类话分开写。** 定义（用符号写清楚是哪个空间、什么维数）、定理（给证明，或引教材到章节）、经验结论（引论文到小节，说明实验条件，并明确写“这不是定理”）。把经验结论写成定理的语气是最常见的错误。
2. **术语第一次出现先定义再用。** “秩”“子空间”“瓶颈”“低秩”都不能裸用。
3. **禁用**“显然”“众所周知”“容易证明”“不难看出”。
4. **每个数字有来源。** 实测的写机器和配置；文献的写到章节。
5. **直觉解释放在严格表述之后**，并标“直观上”。
6. **脚注标记别贴在节号或数字后面**——`§4.1[^ref]` 会渲染成 `§4.1¹`，读者分不清上标是引用还是节号的一部分。写成 `论文[^ref] §4.1`。
7. **参考文献用脚注** `[^key]`，文末列出，写到章节/小节，**每条必须带可点的链接**：arXiv 给 abs 页，期刊给 DOI，会议给 OpenReview/ACL Anthology 页，书给出版社页，讲义给原 PDF。让读者自己搜的参考文献不算写完。

反例（day00 初版）：“任何秩 ≤ r 的矩阵都存在这样的分解，这是秩的等价定义”——是定理但没证；“修正只能落在一个 16 维子空间里”——没说是哪个空间的子空间；“LoRA 的赌注是 ΔW 是低秩的”——把经验假设写成了前提。修正版见 day00 §2.2。

## 文风：别写出 AI 味

参考 HF LLM Course、PyTorch Tutorials、fast.ai 的写法：**直说这节干什么、列出读者会做的事、说清结束时手上有什么。** 不铺垫、不升华。

被用户点名过的毛病：

1. **对仗式排比**——“不是 X，而是 Y”“目标不是学会什么，是建立一个……”。技术上确实要做对比时可以用（“不是降频，是复位”有信息量），但不要拿它当句式装饰。
2. **抽象名词收尾**——“落点”“既成事实”“真正意义”“本质上是”。换成具体的：多少 GB、几分钟、什么会报错。
3. **每段都要总结升华**。写完事实就停。
4. **第一人称抒情**。教程写读者会做什么，不写作者的感想。

一段开头的正确写法：这一天做什么 → 你会做这几件事（列表）→ 跑完手上有什么。

## 写教程的标准

- **动手**一节要逐条命令、可复现，别人 clone 下来能跑
- 数学要用 LaTeX（`$...$` / `$$...$$`），站点用 KaTeX 渲染，GitHub 也原生支持。**不要用表格糊弄矩阵运算**
- 代码放 `code/`，README 里只贴关键的十几行
- 用 Material 的提示框标重点：`!!! note` / `!!! warning` / `!!! danger` / `!!! tip`
- 踩坑一节最有价值，别省

## 硬件安全（有 Jetson 才相关；用独显跳过本节）

课表的实测数字来自 **Jetson AGX Thor**，但内容不绑定它——任何 CUDA GPU 都能跟大部分课。

如果用户用的是 Jetson：到达热保护点是**硬件复位或关机，不是降频**，而各型号的临界温度不同，所以脚本**从内核读这块板子自己的 trip point** 再留余量，不要写死数字。

```bash
bash common/jetson_preflight.sh                      # 起跑前，任一项 FAIL 就别跑
bash common/jetson_telemetry.sh &                    # 遥测常驻
bash common/jetson_watchdog.sh '<进程正则>' 85 &      # >=85C 自动优雅停
bash common/jetson_stop.sh '<进程正则>'               # 要停的时候
```

**绝不**：改功耗/温度限制、对碰 `/dev/nvidia*` 的进程用 `kill -9`、重启、开 persistence mode、跑没有上界的 job。完整规程见 [SETUP.md](SETUP.md#thor-安全)。

## 文档站

站点用 **Quarto**（fast.ai 和 mlabonne 的 LLM Course 同款）。`site_src/` 下的 markdown 和 `_quarto.yml` 全部由 `scripts/build_docs.py` 生成，**不要手改，也不要 commit**。内容源头是根目录的 README/ROADMAP/SETUP/RESOURCES/AGENTS 和 `days/*/README.md`。push 到 main 后 GitHub Actions 自动部署。

本地预览：

```bash
scripts/preview.sh   # http://localhost:4200，改根目录 md 后重跑 build_docs.py
```

**写数学：** 行内 `$...$`，行间 `$$...$$`。**行间公式如果在列表项里，必须缩进 4 空格**，否则会终止列表——这是 markdown 的规则，换任何框架都一样。中文和行内公式之间要留一个空格。

**提示框：** 源码用 GitHub alert 语法（`> [!NOTE]`），构建时转成 Quarto callout。标题写在下一行 `> **标题**`。
